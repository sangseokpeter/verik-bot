[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.pillow", "python311Packages.requests"]

[phases.build]
cmds = ["npm install"]

[phases.start]
cmd = "node src/index.js"
